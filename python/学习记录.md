input函数：获取用户输入，输入字符串
int() float()函数转换字符串
print() 函数默认带换行符 print(, end=" ") print("xxx", '\n' , "xxx", sep="")
python大小写敏感

python语法采取缩进方式，使用缩进表示代码块

注释符 #(单行) 
'''或"""(多行字符串，函数或者类说明，在函数外python会自动不处理，所以可以用于注释)

元组 tuple()  () 相当于只读的列表
列表 list() []
集合  set() {}
字典 dict() { 'xxx': 'xxx' }

enumerate类，返回下表和成员的迭代器，for循环中获取下标和值的元组

字符串前缀
f"xxx{xxx}xxx" 格式化字符串
r"xxxx\n\r" 原始字符串，不做转义

字符串
''或者"" 单行字符串，多行需要使用\n连接，会有空格
''' 或者''' 多行字符串，有空格
使用方法去除textwrap.dedent(raw_text).strip()空格，strip()可以去除空行

数据类型
整数 数字之间允许_分隔，便于阅读
浮点数  e指代10  1.23x109即1.23e9
字符串
布尔值  True False;  布尔值运算符 and or not
空值 None (不等同于0)
 
常量
变量名必须是大小写英文、数字和_的组合，且不能用数字开头
变量名是对对象的引用
 
不可变对象：数字类型(int float complex) 序列类型中"特殊成员"(str字符串 tuple元组 bytes字节串) 其他不可变类型(bool frozenset冻结集合 NoneType空类型None)
不可变对象的共享机制  不可变对象尽量指向相同值对象，节省内存

