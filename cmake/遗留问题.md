为什么在 add_custom_command 中尽量避免使用绝对路径？（涉及构建目录迁移和跨机编译）
如果两个 Target 互相依赖（Circular Dependency），CMake 如何处理？
在交叉编译时，如何让 CMake 优先查找 SDK 里的库而不是宿主机的库？（涉及 CMAKE_FIND_ROOT_PATH_MODE_LIBRARY）

C. 源码学习（最好的参考）
不要只看教程，去看顶级开源项目的 CMakeLists.txt 是怎么写的：
LLVM / Clang：极其复杂的构建体系，学习它如何管理大量模块。
OpenCV：学习它如何处理极度复杂的第三方依赖和交叉编译。
Qt6：看他们如何从 QMake 迁移到 CMake，以及如何利用属性（Property）驱动构建。

B. 深度博客/专栏
Craig Scott 的博客：他是 CMake 维护者之一，他的文章会对 add_custom_command 这种深坑做极细致的拆解。
It's an Hare's World：有很多关于 CMake 性能优化和底层细节的文章。
