最近很想写一写编译定位的经验，总结下通用方法。当然，是自己的通用方法了，经验之谈。

问题类型占比
90%都是拼写(新手错误，老手也可能会犯)
源代码拼写错误是其中占比最大的一部分了，可以说90%以上都是开发自己拼写单词错误导致的，各种报错基本都是这类。

9%是头文件和链接问题
基础的链接问题，也是编译问题的重灾区了，大部分的开发都不关注编译链接原理，导致出现各种让人无语的链接问题。

1%剩下的真正问题
剩下的这1%才是真正的编译问题了

当然，这是建立在比较健壮的编译框架构建上的。稀烂的构建框架，99%的垃圾问题中比例可能有变化，但是1%占比基本是不变的。

C代码的话主要从两类构建脚本来说说吧，cmake和makefile，主要是cmake为例子了。其他的ninja或者其他的虽然也是主流，但是没有这俩广，主要是我工作中没怎么用过，不带熟悉，哈哈。

定位三要素，主要是这三个，疑难问题可能就要借助其他手段了。
1. 日志
makefile和cmake(generator makefile风格，这个是默认值)编译时候都会打印编译的日志，日志是定位的第一手信息
make和cmake编译时默认是打简要信息。
make加V=1可以开启详细log；cmake如果用--build编译加-v就可以了，如果用make执行编译加VERBOSE=1。
如果常规日志打印信息足够，不建议打印详细日志，开启详细打印会多很多信息，一般开启这个时为了看执行命令的。

2. 代码本身(包含源码和构建代码)
日志可以定位到哪个代码报错，但是为什么报错是需要结合代码本身来进一步定位的。
用户态代码的编译，代码主要是源代码和构建脚本了。
内核态会有点不一样，还有包含linux的内核目录树。

3. 中间文件
当问题比较复杂，日志和代码结合都定位不清楚的时候，就可以取中间文件再继续看一看了。
中间文件很丰富的，有.o文件，有.so，有.a，有可执行程序，.a、.so、可执行程序没有正式发布都算中间文件，因为.so、.a、可执行程序发布前还可能做符号裁剪等操作。
对cmake而言，中间文件还有缓存文件，编译和链接命令存放的文件，生成的makefile文件，这也是很好的定位信息输入。
如果是内核态的编译，中间文件还包括内核编译过程中的一些.cmd.o等文件，还有ko文件，.o文件等

上面说了百分比占比和定位要素，下面来说说常规解决定位路径吧。

先看用户态吧
1. 99%常规问题常规定位
Error 1 -> target -> .c
对于常规的报错，定位比较简单，对着报错的log找第一案发现场
一般搜索Error 1就能找到，找哪个target报错，看报错的target的上下文，看编译器的具体报错。
因为工业代码编译的内容多，一般不会只报错一处，基本上都是找第一处报错的，然后逐个解决。
makefile风格构建脚本编译，构建依赖就是棵树，所以报错是一层层的，搜索error 1找最外层，然后往里看就可以了。

找到错误现场，怎么继续看对新手来说其实也是老大难。报错不复杂，就在Error 1上面一点点直接指出来的话，那是最理想的情况了。
但，没那么好，有的问题报错一大堆，新手无从看起。
如果是编译问题，一般搜索error:能直接找到报错的位置，但是不建议直接上来搜索这个，因为我们要知道报错的链路，正向解决问题，而不是后续靠玄学定位。
还有一类是hi链接问题，搜索no found、needed by、undereference基本上也能找到报错的地方。
除了这两类通性的问题，其他的就要耐着性子从Error 1网上继续看一看，找具体的报错点了。

2. 1%的真正问题
